# Benedict Hospital Management System

Double-click **Open Hospital System.bat** to start the hospital dashboard and open it in your browser.

The application lives in `hospital-app` and is based on `hospital db schema.sql`.

Manual start:

```powershell
cd hospital-app
npm install
npm run dev
```

Open http://127.0.0.1:5173/.
